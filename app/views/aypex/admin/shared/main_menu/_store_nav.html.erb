<% if can?(:admin, current_store) %>
  <div class="dropdown-menu overflow-hidden p-0" aria-labelledby="storeSelectorDropdown">
    <% @stores.each do |store| %>
      <%= store_switcher_link(store) %>
    <% end %>

    <% if can?(:create, Aypex::Store) %>
      <div class="dropdown-divider m-0"></div>
      <%= link_to aypex.new_admin_store_path, class: 'p-3 m-0 dropdown-item', id: 'addNewStoreLink' do %>
        <%= aypex_admin_svg_tag "plus-circle", size: '18px * 18px' %> <%= I18n.t("aypex.admin.add_new_store") %>
      <% end %>
    <% end %>
  </div>
<% end %>
