<ul class="nav nav-sidebar" id="sidebarDashboard">
  <%= tab I18n.t('aypex.admin.tab.dashboard'), url: aypex.admin_path, match_path: '/dashboard', icon: 'card-heading.svg' %>
</ul>

<% if can? :admin, Aypex::Order %>
  <ul class="nav nav-sidebar" id="sidebarOrder">
    <%= main_menu_tree I18n.t('aypex.admin.tab.orders'), icon: "inbox-fill.svg", sub_menu: "orders", url: "#sidebar-orders" %>
  </ul>
<% end %>

<% if can?(:admin, Aypex::ReturnAuthorization) || can?(:admin, Aypex::CustomerReturn) %>
  <ul class="nav nav-sidebar" id="sidebarReturn">
    <%= main_menu_tree I18n.t('aypex.admin.tab.returns'), icon: "reply-fill.svg", sub_menu: "returns", url: "#sidebar-returns" %>
  </ul>
<% end %>

<% if can? :admin, Aypex::Product %>
  <ul class="nav nav-sidebar" id="sidebarProduct">
    <%= main_menu_tree I18n.t('aypex.admin.tab.catalog'), icon: "tags-fill.svg", sub_menu: "product", url: "#sidebar-product" %>
  </ul>
<% end %>

<% if can?(:admin, Aypex::StockLocation) || can?(:admin, Aypex::StockTransfer) %>
  <ul class="nav nav-sidebar" id="sidebarStock">
    <%= main_menu_tree I18n.t('aypex.admin.tab.stock'), icon: "box-seam.svg", sub_menu: "stock", url: "#sidebar-stock" %>
  </ul>
<% end %>

<% if can? :admin, Aypex::Admin::ReportsController %>
  <ul class="nav nav-sidebar" id="sidebarReport">
    <%= tab *Aypex::Admin::Configuration::REPORT_TABS, icon: 'pie-chart-fill.svg' %>
  </ul>
<% end %>

<% if can? :admin, Aypex::Promotion %>
  <ul class="nav nav-sidebar" id="sidebarPromotion">
    <%= main_menu_tree I18n.t('aypex.admin.promotions.promotions'), icon: "percent.svg", sub_menu: "promotion", url: "#sidebar-promotions" %>
  </ul>
<% end %>

<% if defined?(Aypex::Vendor) && can?(:manage, Aypex::Vendor) && can?(:index, Aypex::Vendor) %>
  <ul class="nav nav-sidebar" id="sidebarOrganizations">
    <%= main_menu_tree I18n.t('aypex.admin.promotions.organizations'), icon: "building.svg", sub_menu: "organizations", url: "#sidebar-organizations" %>
  </ul>
<% end %>

<% if Aypex.user_class && can?(:admin, Aypex.user_class) %>
  <ul class="nav nav-sidebar" id="sidebarUser">
    <%= tab *Aypex::Admin::Configuration::USER_TABS, url: aypex.admin_users_path, icon: 'people-fill.svg' %>
  </ul>
<% end %>

<% if can? :admin, Aypex::Menu %>
  <ul class="nav nav-sidebar" id="sidebarMenu">
    <%= main_menu_tree I18n.t('aypex.admin.tab.content'), icon: "card-heading.svg", sub_menu: "content", url: "#sidebar-content" %>
  </ul>
<% end %>
