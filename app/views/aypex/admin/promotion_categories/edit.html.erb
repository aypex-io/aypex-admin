<% content_for :breadcrumb do %>
  <%= breadcrumb_builder(link_one_uri: aypex.admin_promotion_categories_path, link_one_text: I18n.t("aypex.admin.all_promotion_categories"), current_page_name: @promotion_category.name) %>
<% end %>

<%= form_with model: [:admin, @promotion_category], data: {controller: "form-state"} do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'aypex/admin/shared/resource_actions' %>
  </fieldset>
<% end %>
